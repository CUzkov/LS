version: '3.3'

services:
  redis:
    build: ./backend/redis
    ports:
      - 6379:6379
  
  database:
    build: ./backend/database
    network_mode: host
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=ls
    
  # server:
  #   build: ./backend/server
  #   network_mode: host